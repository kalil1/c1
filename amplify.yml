version: 1
frontend:
  phases:
    preBuild:
      commands:
        - gem install bundler
        - rm -f Gemfile.lock
        - sudo yum list postgresql91\*
        - bundle install
        - yarn install
        - rails db:seed
    build:
      commands:
        - RAILS_ENV=production bundle exec rails assets:precompile
        - RAILS_ENV=production bundle exec rails db:migrate
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - vendor/bundle/**/*
      - tmp/cache/**/*
